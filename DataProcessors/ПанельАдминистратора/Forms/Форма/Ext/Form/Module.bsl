
&НаКлиенте
Процедура УдалитьЗанятие(Команда)
	Если НЕ ЗначениеЗаполнено(Занятие) Тогда Возврат; КонецЕсли;
	
	УдалитьЗанятиеНаСервере(Занятие);
КонецПроцедуры

&НаСервере
Процедура УдалитьЗанятиеНаСервере(ЗанятиеСсылка)
	
	ОбъектЗанятие = ЗанятиеСсылка.ПолучитьОбъект();
	ОбъектЗанятие.Удалить();
	
КонецПроцедуры
